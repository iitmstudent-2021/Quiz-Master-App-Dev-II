redis-server---->  redis-server(sudo systemctl stop redis)

worker--->  celery -A app.celery worker --loglevel INFO

python_flask server----->  python3 app.py

celery beat server----->  celery -A app.celery beat --loglevel INFO

virtual environment----->  python3 -m venv venv

activating it-----> source venv/bin/activate

pip install -r requirements.txt

pip freeze > requirements.txt

rm -rf venv

pip install setuptools

